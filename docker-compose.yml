version: '3'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - MONGO_URL=mongodb+srv://claraema7:Claraema1717@ecommercetparqweb.xaaj4yo.mongodb.net/shop?retryWrites=true&w=majority
      - PORT=3000
    depends_on:
      - mongo

  mongo:
    image: mongo:4.4
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: